<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <!-- Style BTN -->
    <style name="ButtonYellow" parent="Theme.AppCompat.Light">
        <item name="colorControlHighlight">@color/ButtonYellowAccent</item>
        <item name="colorButtonNormal">@color/ButtonYellowPrimary</item>
    </style>

    <style name="ButtonGray" parent="Theme.AppCompat.Light">
        <item name="colorControlHighlight">@color/ButtonGrayAccent</item>
        <item name="colorButtonNormal">@color/ButtonGrayPrimary</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

</resources>
